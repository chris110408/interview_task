import styled from "styled-components";

export const DetailDiv = styled.div`
  p {
    position: relative;
    margin: 15px;
    padding-left: 36px;
    font-size: 20px;
    &:last-child {
      margin-bottom: 0;
    }
  }

  i {
    position: absolute;
    top: 6px;
    left: 0px;
  }
`;
